# Archived Docs

These documents are kept only for project history.

They describe earlier experiments, phased setup notes, or architectures that no longer match the current implementation. Do not use them as the source of truth for the live app.

Use the root `README.md` as the canonical project guide.
